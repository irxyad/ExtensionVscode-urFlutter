// import { expect } from "chai";
// import { countText } from "../common/utils/stringUtils";

// describe("Text Analyzer", () => {
//   it("should count characters and words", () => {
//     const result = countText("Hello world from VSCode");
//     expect(result.characters).to.equal(23);
//     expect(result.words).to.equal(4);
//   });

//   it("should handle empty string", () => {
//     const result = countText("");
//     expect(result.characters).to.equal(0);
//     expect(result.words).to.equal(0); // atau 1 kalau lo mau default
//   });
// });
